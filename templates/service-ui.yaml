{{- if .Values.ui.enabled -}}
apiVersion: v1
kind: Service
metadata:
  name: terrakube-ui-service
spec:
  type: ClusterIP
  ports:
  - port: 8080
    targetPort: 8080
  selector:
    app: terrakube-ui
{{ end }}